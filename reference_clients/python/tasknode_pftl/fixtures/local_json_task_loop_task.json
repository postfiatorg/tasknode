{
  "schema": "pf.task.local_json_loop.v1.task_fixture",
  "title": "Create local JSON task loop CLI",
  "description": "Build a deterministic Python CLI that generates a task, accepts it, submits an artifact, reviews the submission, and updates local reputation state.",
  "task_kind": "engineering_demo",
  "steps": [
    "Create a canonical request bundle and generated task.",
    "Persist the accepted task and evidence artifact to local JSON storage.",
    "Review the artifact and update reputation state deterministically."
  ],
  "submission_requirement": {
    "type": "text",
    "criteria": "Submit the local demo receipt and evidence artifact text showing deterministic IDs, JSON persistence outputs, and the final reputation update."
  },
  "reward_offer": {
    "amount_pft": "3.200000",
    "unit": "PFT"
  }
}
