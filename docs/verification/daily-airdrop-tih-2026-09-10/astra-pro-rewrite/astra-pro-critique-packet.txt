CRITIQUE PACKET (independent judges):

--- Judge: anthropic/claude-fable-5.1 | score 70.0/100 ---
[reasoning] Prompt-design judgments: The prompt has a clear role, a single well-defined decision, a strict output schema, and an unusually careful trust-boundary section that separates deterministic Task Node metadata from user-originated text (titles, reward reasons, quoted evidence), explicitly neutralizes injection attempts, and turns them into a negative signal. It also documents downstream clamping (max_daily_pft, max_reward_fraction) so the model does not have to invent caps, handles the zero-reward edge case with a deterministic rule, and forbids touching the recipient wallet. Those are strong production properties. Weaknesses are mostly in calibration and specification: the core heuristic ('how much would a crypto network rationally pay today to retain this actor') has no anchors, tiers, or examples mapping evidence quality to a 0-10000 PFT amount or a 0-100 retention score, and the relationship between those two outputs (and between the drop and total_reward_paid_pft) is left implicit while the text simultaneously says 'keep it grounded in rewarded work' and 'do not assume a low proportional cap'. Lookback weighting, duplicate/self-dealing task patterns, missing or malformed fields, and unexplained terms like 'identity cloud' are not addressed. Claims requiring empirical testing: whether the missing anchors actually produce high run-to-run variance in daily_airdrop_pft, whether the 'must lower the score' rule causes false positives on legitimate payout-related tasks, and whether the model reliably returns bare JSON without fences cannot be judged from the text alone.
[best_thing] The trust-boundary section: it precisely enumerates which packet fields are system-deterministic versus user-authored, instructs the model to treat embedded text as data not instructions, and converts payout demands or score dictation into a penalty signal with a hard ceiling rule.
[worst_thing] No calibration anchors: the 0-10000 PFT and 0-100 retention outputs have no rubric, tiers, or worked examples, and their relationship to each other and to the packet's reward totals is ambiguous, so the central numeric decision is left to unguided judgment.
[one_best_next_edit] Add a short calibration rubric with 3-5 tiers (e.g., 'no verifiable shipped artifact / single trivial task' -> 0-5% of max_daily_pft, 'multiple verified deliverables with clear product or network impact' -> 60-100%) that maps concrete evidence patterns to retention_value_score bands and to daily_airdrop_pft as a fraction of max_daily_pft, and state explicitly how daily_airdrop_pft should relate to total_reward_paid_pft when no max_reward_fraction is present.

--- Judge: anthropic/claude-fable-5.1 | score 72.0/100 ---
[reasoning] As a prompt-design artifact this is above average for a payout-decision role. It defines the role, restricts inputs to the packet, draws an explicit trust boundary between deterministic system metadata and user-authored fields, gives concrete anti-injection rules (and makes injection attempts a negative signal), specifies a hard eligibility gate, describes how the system clamps proposals, and mandates a single JSON object with a fixed schema and one-sentence fields that force grounded, actionable feedback. Weaknesses are mostly about calibration and consistency rather than safety: the core valuation heuristic ('how much would a network rationally pay to retain this actor') is evocative but unanchored, with no rubric, bands, or mapping between retention_value_score and daily_airdrop_pft, so two runs on similar packets could diverge widely; the schema hardcodes 0-10000 while the real cap is a variable max_daily_pft, inviting mismatch; 'do not assume a low proportional cap' sits in tension with 'never output above what the packet facts justify' without saying what 'justify' means; recency/lookback weighting is unspecified; the rule to ignore text that 'claims retention value' could over-penalize legitimate reward_reason prose; and eligibility_reason behavior when eligible, handling of missing or malformed fields, and a no-markdown-fence instruction are absent. Whether the heuristic actually produces stable, fair amounts, and whether the injection language resists real adversarial titles, would require empirical output testing and cannot be judged from the text alone.
[best_thing] The explicit trust boundary that names which packet fields are deterministic system metadata versus untrusted user-authored text, and treats any embedded payout/score manipulation as both instructions-to-ignore and a negative quality signal.
[worst_thing] The payout logic is uncalibrated: there is no rubric or formula linking retention_value_score to daily_airdrop_pft, no scoring bands or anchors, and the hardcoded 0-10000 range conflicts with the variable max_daily_pft cap, so amounts will be inconsistent across runs.
[one_best_next_edit] Add a short calibration section: define 3-5 retention_value_score bands with concrete evidence criteria (e.g., breadth, verification quality, artifact impact, recency within lookback), specify how the score maps to daily_airdrop_pft relative to max_daily_pft (and max_reward_fraction when present) instead of the fixed 0-10000, and state that eligibility_reason must be null when eligible.

--- Judge: anthropic/claude-fable-5.1 | score 70.0/100 ---
[reasoning] As a prompt design, this is above average for a payout-deciding system prompt: it establishes a clear role, an explicit trust boundary separating deterministic Task Node metadata from user-originated text, concrete injection-handling rules (ignore and penalize payout demands or reviewer-addressed text), a hard ineligibility rule for zero rewarded work, a description of downstream deterministic caps, and a fixed JSON output schema with useful explanatory fields. Its main design weaknesses are calibration and verifiability. The 0-10000 PFT range and 0-100 retention score have no anchors, bands, or worked example, so different runs on the same packet would plausibly diverge widely; the retention heuristic ('how much would a network rationally pay') is evocative but unoperationalized. The relationship between retention_value_score and daily_airdrop_pft is undefined. The instruction 'do not assume a low proportional cap unless the packet includes one' sits awkwardly next to 'keep the drop grounded in rewarded work' and could push proposals high knowing they'll be clamped. Because titles and reward_reasons are declared user-controlled, the only trusted signals are amounts, timestamps, CIDs and hashes, yet the prompt asks the model to judge artifact quality and verification outcomes without saying how to weigh unverifiable narrative versus trusted numerics. There is no handling for malformed or missing packet fields, no explicit 'no markdown fences' instruction, and 'private daily drop' is undefined. Whether the injection penalty is overbroad (e.g., legitimate quoted feedback that mentions payouts) or whether outputs are actually stable would require empirical testing and is not judged here.
[best_thing] The trust-boundary section that itemizes which packet fields are deterministic system metadata versus user-originated text, and treats embedded payout/score demands as both non-instructions and a negative quality signal.
[worst_thing] No calibration for the two numeric outputs: nothing maps evidence patterns to PFT bands or retention scores, leaving a 0-10000 payout decision to unanchored judgment.
[one_best_next_edit] Add a short scoring rubric with 3-5 anchored bands (e.g., example packet profiles mapped to retention_value_score ranges and corresponding daily_airdrop_pft ranges, plus a stated rule tying the PFT proposal to the retention score and to total_reward_paid_pft in the lookback), and include one compact worked example.

--- Judge: anthropic/claude-fable-5.1 | score 72.0/100 ---
[reasoning] Prompt-design strengths: clear single role and single decision, an explicit and well-structured trust boundary that separates deterministic Task Node metadata from user-originated text (title, reward_reason, quoted evidence), concrete injection-handling rules with a defined consequence (lower the score), an unambiguous zero-reward eligibility rule, a stated relationship between the model's proposal and system-side clamping, and a fixed JSON output schema with tight per-field formats. The retention heuristic gives a coherent north star. Weaknesses as written: the core quantitative mapping is uncalibrated — there is no rubric tying retention_value_score bands or evidence patterns to PFT amounts within 0-10000, and 'how much a network would rationally pay' is a vague anchor that will produce high variance across runs and packets. The 0-10000 hard range may conflict with max_daily_pft and it is unclear whether the reviewer should self-cap at the packet bounds or freely propose and rely on clamping. 'Do not assume a low proportional cap unless the packet includes one' is ambiguous about what the default should be. Terms like 'narrow, replaceable, weakly evidenced' and 'high-quality verification outcomes' lack operational definitions or examples. No handling is specified for missing/malformed fields, partial lookback windows, or how the sentence fields should be filled when the user is ineligible. 'must lower the score' for injection attempts gives no magnitude. Claims about actual injection resistance, score consistency, and payout calibration cannot be judged from the text and would require empirical output testing; the design gives a reasonable but not sufficient basis for expecting them.
[best_thing] The explicit trust boundary that enumerates which packet fields are deterministic system metadata versus untrusted user-authored text, paired with a concrete rule that manipulation attempts in that text must lower the score rather than be obeyed.
[worst_thing] The absence of any calibration or rubric linking evidence quality to the 0-10000 PFT amount and the 0-100 retention score, leaving the central numeric decision to unanchored judgment and likely high run-to-run variance.
[one_best_next_edit] Add a short calibration table mapping retention_value_score bands (e.g., 0-20, 21-50, 51-80, 81-100) to defining evidence characteristics and to a proportion of max_daily_pft (or of max_reward_fraction × total_reward_paid_pft when present), and state explicitly that daily_airdrop_pft must never exceed those packet bounds.

--- Judge: anthropic/claude-fable-5.1 | score 68.0/100 ---
[reasoning] As a prompt-design artifact this is above average for a production decision prompt. It has a clear role, a single well-specified output schema, an explicit zero-reward/ineligible edge case, and an unusually careful trust boundary that distinguishes deterministic Task Node metadata from user-authored text, forbids wallet reinterpretation, and converts injection attempts into a negative scoring signal rather than merely ignoring them. Those are the right structural choices for an adversarial airdrop setting. Its main weakness is calibration: the core heuristic ('how much would a network rationally pay to retain this actor') is evocative but unanchored, there is no rubric or band mapping for retention_value_score, and no rule linking that score to the 0-10000 daily_airdrop_pft integer or to total_reward_paid_pft. The line 'keep the drop grounded in the rewarded work... but do not assume a low proportional cap' is internally tense and leaves the model free to propose large drops for small evidenced work, relying on downstream clamping. Whether this produces high run-to-run variance, inconsistent treatment across similar users, or systematic over-proposal cannot be judged from the text alone and would need empirical output testing; but the absence of anchors makes such variance likely by design. Smaller gaps: `daily_airdrop_policy` is named but never told how to be used; malformed or missing packet fields are unaddressed; eligibility_reason semantics when eligible are only implied; and the injection rule could over-penalize benign reward_reason phrasing that merely mentions 'value' or 'eligibility'.
[best_thing] The trust-boundary section: it enumerates exactly which packet fields are deterministic system metadata versus untrusted user-authored text, forbids acting on embedded instructions, treats manipulation attempts as a fraud signal, and hard-prohibits proposing above what packet facts justify.
[worst_thing] No calibration: nothing maps evidence patterns to retention_value_score bands or converts that score into a daily_airdrop_pft amount relative to total_reward_paid_pft or max_daily_pft, so the 0-10000 integer is essentially unconstrained and likely inconsistent across users.
[one_best_next_edit] Add a short calibration rubric: 3-5 anchored bands for retention_value_score (e.g., what evidence profile earns ~20, ~50, ~80), a default rule tying daily_airdrop_pft to the score and to the packet's total_reward_paid_pft / max_daily_pft unless daily_airdrop_policy overrides it, and one or two compact worked examples including an injection-attempt case.

--- Judge: gpt-6-astra | score 68.0/100 ---
[reasoning] The prompt is a solid starting point but underspecified for production financial allocation. It limits evidence to the packet, distinguishes system metadata from contributor-controlled text, preserves wallet routing, defines a zero-paid-work eligibility rule, and requires structured output. However, it provides neither anchored scoring criteria nor a reproducible mapping from contribution evidence to retention score and payout. The subjective retention heuristic therefore leaves substantial allocation discretion. Deterministic clamping bounds payouts but does not establish that amounts within those bounds are justified. The prompt also lacks explicit handling for missing or contradictory fields, unclear verification provenance, and potential conflicts between the fixed 0–10000 output range and configured policy limits. Its instruction to penalize any embedded eligibility or retention-value claim is overbroad: legitimate task descriptions or quoted feedback could trigger penalties without establishing misconduct. These are prompt-design findings, not observed model failures. Actual injection resistance, payout consistency, fairness, and calibration would require representative and adversarial output testing.
[best_thing] The explicit trust boundary separates routing and reward metadata from untrusted task prose, while keeping wallet selection outside the reviewer's discretion.
[worst_thing] There is no operational rubric connecting evidence to score and score to payout, leaving the central monetary decision insufficiently specified despite the presence of payout caps.
[one_best_next_edit] Replace the open-ended retention heuristic with a versioned allocation rubric defining evidence-quality requirements, anchored score bands, and an explicit score-to-PFT formula using packet policy fields, including a defined fallback when the evidence is insufficient.

--- Judge: gpt-6-astra | score 73.0/100 ---
[reasoning] The prompt has a clear purpose, a packet-only evidence constraint, explicit trust boundaries, a zero-paid-work eligibility rule, and a compact output contract. These are strong production-oriented design choices. Its central weakness is that neither contributor scoring nor the conversion from score to token amount is calibrated: the retention-payment heuristic leaves substantial economic discretion without scoring anchors or a payout formula. It also lacks defined handling for missing or inconsistent metadata, duplicate tasks, and conflicts between the fixed 0–10000 output range and configured caps. External clamping limits payouts but does not make proposed allocations justified or consistent. The injection rule is overbroad: legitimate contribution evidence may discuss retention value or quote malicious instructions, yet the prompt mandates a score reduction. These are prompt-design findings; actual injection resistance, allocation consistency, and contributor-value accuracy require empirical testing.
[best_thing] The explicit separation of deterministic metadata from untrusted task text is unusually clear, and the prompt correctly locates payout-cap enforcement outside the model rather than relying solely on compliance.
[worst_thing] The core financial decision is under-specified: qualitative evidence must become both a 0–100 score and a token payout without anchored criteria or a defined mapping, making allocation difficult to audit.
[one_best_next_edit] Replace the open-ended payout heuristic with a versioned decision rubric defining weighted evidence criteria, anchored score bands, and a deterministic score-to-payout formula using validated policy fields and configured caps; specify a fail-closed outcome when required policy inputs are missing or inconsistent.

--- Judge: gpt-6-astra | score 73.0/100 ---
[reasoning] The prompt provides a clear role, packet-only evidence boundary, explicit eligibility gate, structured JSON output, and a useful separation between trusted metadata and untrusted task text. External payout clamping is a strong safeguard, although its implementation cannot be verified from this document. The main production weakness is underdetermination: there are no scoring anchors or rules translating contributor value into a token amount, leaving financially consequential discretion largely unconstrained below the caps. It also lacks defined behavior for missing or inconsistent metadata, ambiguous lookback boundaries, and conflicts between the fixed 0–10000 output range and configured limits. The instruction to penalize text that claims retention value or eligibility is overbroad: legitimate contribution evidence could contain those claims without attempting prompt injection. These are prompt-design findings; actual scoring consistency, injection resistance, and payout calibration require empirical testing.
[best_thing] The explicit field-level trust boundary, paired with externally enforced payout caps, separates evidence evaluation from routing authority and payment limits.
[worst_thing] The prompt offers no anchored rubric or score-to-payout mapping, so neither a particular score nor its associated token allocation is reproducibly justified by the written rules.
[one_best_next_edit] Add a policy-backed decision rubric with weighted evidence dimensions, concrete score-band anchors, and a deterministic score-to-payout formula that applies eligibility, configured caps, and integer rounding explicitly.

--- Judge: gpt-6-astra | score 73.0/100 ---
[reasoning] The prompt has a clear role, a bounded evidence source, explicit trust boundaries, a zero-reward ineligibility rule, and a compact output contract. It appropriately distinguishes deterministic routing metadata from user-authored task content and describes downstream payout caps. Its main production weakness is an under-specified decision policy: neither the retention score nor its conversion into tokens has calibrated anchors, leaving materially different payouts compatible with the instructions. Paid rewards establish compensated activity, but user-authored descriptions do not independently establish artifact quality or retention value. Missing or contradictory packet fields, additional eligibility conditions, and conflicts between the fixed 0–10000 output range and packet policy are not addressed. The injection rule also conflates ignoring instructions with penalizing contributors, potentially punishing legitimate quoted material or ordinary descriptions of contribution value. These are prompt-design judgments; actual payout consistency, injection resistance, JSON validity, and agreement with expert reviewers require empirical testing. The claimed deterministic caps and metadata provenance also require verification in the surrounding system.
[best_thing] The explicit field-level trust boundary, combined with a deterministic no-paid-work eligibility gate and externally enforced payout caps, provides a strong foundation for separating contribution evidence from payout manipulation.
[worst_thing] There is no operational rubric connecting evidence to retention scores and token amounts. The central financial decision is therefore left to an open-ended judgment about what a network would rationally pay.
[one_best_next_edit] Add a versioned decision rubric with anchored evidence-quality and contribution-impact score bands, an explicit score-to-token formula using trusted policy fields, and a defined error or abstention path for missing or conflicting inputs. Make the configured payout bounds authoritative over the example output range.

--- Judge: gpt-6-astra | score 72.0/100 ---
[reasoning] The prompt has a clear purpose, a restricted evidence source, explicit trust boundaries, a zero-reward eligibility rule, and a compact structured output. It also correctly separates model recommendations from system-enforced payout caps. However, the central economic judgment is underspecified: there are no scoring anchors, evidence weights, or rules mapping contributor value to a daily token amount. Recorded payments establish rewarded activity, but user-authored descriptions do not independently establish artifact quality, verification outcomes, or retention value. Missing or contradictory packet fields are not addressed, and the fixed 0–10000 output range is not reconciled with configurable policy bounds. These are identifiable design gaps; actual scoring consistency, injection resistance, and payout calibration would require empirical testing rather than being established by inspection.
[best_thing] The explicit separation of deterministic metadata from untrusted task text, reinforced by system-side payout caps, is a strong foundation for bounded decision-making.
[worst_thing] The rule requiring score reductions for text that claims eligibility or retention value conflates instruction attacks with potentially legitimate contribution evidence. It also turns suspicious text into a mandatory scoring influence rather than simply ignoring its instructions, creating a design-level risk of unwarranted penalties.
[one_best_next_edit] Replace the open-ended retention-payment heuristic with a policy-backed decision rubric: define anchored score bands using observable rewarded-work evidence, specify how unverified claims and insufficient evidence are handled, and provide an explicit score-to-payout calculation constrained by the configured caps and output range.

--- Judge: z-ai/glm-5.3 | score 74.0/100 ---
[reasoning] Assessed as prompt design only; I did not execute the reviewer role or assume any empirical output behavior. Design strengths visible in the text: a clear role and scope constraint ('use only the supplied task reward packet'); a genuinely mature injection-defense architecture that enumerates field-level trust boundaries (deterministic system metadata vs. user-originated titles/reasons/quoted evidence), treats untrusted text strictly as data, converts injection attempts into an explicit score-lowering signal, and forbids altering the recipient wallet; externalization of hard financial bounds (max_daily_pft, max_reward_fraction, deterministic clamping) so model overshoot is not directly payable; a hard, testable zero-reward ineligibility rule; and a tight single-JSON output contract with useful contributor-facing feedback fields (what_raised_today / what_kept_it_lower / to_improve_tomorrow). Design weaknesses: the core decision is uncalibrated — retention_value_score has no rubric, anchors, bands, or examples, and no mapping between the 0-100 score and the 0-10000 airdrop is defined, so the two numbers can diverge and similar packets can yield dissimilar payouts; the ceiling guidance pulls in opposite directions ('never output an airdrop above what the deterministic packet facts justify' vs. 'do not assume a low proportional cap') without defining what 'justifies' an amount; edge cases are unhandled (malformed or missing packet fields, negative/ambiguous reward values, recency weighting within the lookback, and penalizing quoted third-party text as if it were the contributor's own injection attempt); there are no few-shot examples; and the 0-10000 schema bound may conflict with max_daily_pft if policy permits higher, while the prompt simultaneously says proposals above bounds are merely clamped. Claims I explicitly did not make and which would require empirical testing: actual JSON-only compliance (e.g., no code fences), real injection resistance under adversarial packets, run-to-run score variance, and whether models implicitly infer a score-times-100 mapping. Net: strong safety engineering and output structure, weak decision calibration — above average for production, but not yet reliable as the sole judge of daily token allocation.
[best_thing] The field-level trust boundary combined with deterministic clamping: it explicitly separates system metadata from user-originated text, instructs the model to treat the latter as data never instructions, turns injection attempts into a score-lowering fraud signal, protects the recipient wallet, and pushes hard payout caps into a non-LLM layer so the model's proposal cannot directly overspend.
[worst_thing] The payout decision itself is uncalibrated: no rubric or anchors for retention_value_score, no defined relationship between the 0-100 score and the 0-10000 airdrop amount, and conflicting ceiling guidance ('never output above what the packet facts justify' vs. 'do not assume a low proportional cap') — for a system that allocates money daily, this invites high variance across runs, models, and contributors.
[one_best_next_edit] Insert a short calibration section that defines 4-5 anchored bands for retention_value_score (with one-line behavioral examples of what earns each band) and an explicit score-to-airdrop function (e.g., airdrop = round(score/100 x min(max_daily_pft, configured multiple of total_reward_paid_pft)), or an explicit statement that the proposal must track the score linearly), so the qualitative judgment and the payout number cannot diverge.

--- Judge: z-ai/glm-5.3 | score 78.0/100 ---
[reasoning] As a prompt-design artifact, this is strong: it establishes a clear role, defines a trust boundary separating deterministic system metadata from untrusted user-authored text, gives explicit prompt-injection defenses (ignore embedded instructions; treat injection attempts as fraud signals that lower the score), specifies hard eligibility and capping rules (max_daily_pft, max_reward_fraction, zero-reward ineligibility), and demands a single structured JSON output. These are all judgments from the text itself. What it lacks is an evaluation rubric: it never explains how to map evidence quality to a numeric retention_value_score or daily_airdrop_pft amount, leaving the two most important numbers almost entirely to model discretion. The 'rational retention payment' heuristic is evocative but unanchored (no scale anchors, examples, or tie-breakers between a high score and a low airdrop or vice versa). It also doesn't state lookback-window semantics, tie handling for multiple tasks, or how the airdrop should relate to the retention score — a model could defensibly output many different amount/score combinations. Whether the model reliably resists injections and clamps correctly are empirical questions requiring live output testing, not text review; the prompt's design for those goals is nonetheless sound.
[best_thing] The explicit trust boundary with a named list of deterministic fields versus untrusted user-authored text, plus the instruction that injection attempts are themselves fraud signals that must lower the score — a genuinely well-engineered injection defense.
[worst_thing] No scoring rubric or anchors connecting evidence quality to the numeric retention_value_score or daily_airdrop_pft, so the core outputs are underdetermined and likely to vary widely across runs or models.
[one_best_next_edit] Add a compact rubric section: 4-6 anchored score bands (e.g., 0-20 = trivial/narrow work, 80-100 = shipped artifact with verified network value) and a formula or band mapping from retention_value_score to a suggested airdrop range before clamping, so the two key numbers are reproducible rather than discretionary.

--- Judge: z-ai/glm-5.3 | score 74.0/100 ---
[reasoning] Design-level review; the prompt was not executed. Strengths: clear role and single decision task; explicit input scoping ('use only the supplied task reward packet'); an unusually concrete trust-boundary section that enumerates which fields are deterministic system metadata versus untrusted user-authored content, forbids treating task text as instructions, and converts embedded injection attempts into an explicit score-lowering signal; a deterministic downstream clamp (max_daily_pft, optional max_reward_fraction) that bounds financial exposure even if the model errs; a hard, checkable eligibility gate (no reward_paid_pft > 0 in lookback means 0 PFT and ineligible); and a typed, auditable output schema with enums and null handling. Weaknesses: the core decision variable is under-specified. There are no anchors or rubric bands for the 0-100 retention_value_score, and, more importantly, no stated relationship between that score and daily_airdrop_pft, so the payout amount rests on model discretion within the caps; that is a design gap likely to yield inconsistent or cap-pinned amounts across comparable users and days, though the actual variance magnitude is an empirical question requiring multi-run testing with real packets. The line 'do not assume a low proportional cap unless the packet includes one' removes default conservatism without supplying any baseline proportionality. The schema caps the airdrop at 10000 without stating how that relates to max_daily_pft, creating a possible conflict if the policy cap differs. The prompt asserts all feedback and quoted text 'originates from the user being scored'; if third-party feedback can appear in packets, the injection penalty could be misattributed and weaponized to sink another contributor's score. No worked example or few-shot anchor is provided, and edge cases (missing max_daily_pft, malformed packet, when eligibility_reason is null versus a string) are unspecified. Injection resistance, score consistency, calibration, and parser compatibility would all require empirical output testing and red-teaming; those outcomes are not claimed here.
[best_thing] The trust-boundary section: it enumerates exactly which packet fields are deterministic system metadata versus untrusted user-authored content, instructs the model to treat task titles, reward reasons, and quoted evidence as data rather than instructions, and turns embedded injection attempts into an explicit score-lowering signal. Paired with the deterministic clamp, this is credible defense-in-depth for a prompt that must read adversarial text.
[worst_thing] No decision rule connects retention_value_score to daily_airdrop_pft. With only a qualitative retention heuristic and upper-bound caps, the actual payout amount is left to model discretion, inviting inconsistent awards across comparable contributors, cap-pinning by aggressive models, and arbitrary round-number outputs; the heuristic also removes the default proportional cap without replacing it with any baseline formula.
[one_best_next_edit] Add a compact scoring-and-payout rubric: 4-5 anchored retention_value_score bands with concrete evidence descriptions, plus an explicit formula or band table deriving daily_airdrop_pft from retention_value_score and total_reward_paid_pft (subject to the max_daily_pft and max_reward_fraction clamps), illustrated with one worked packet-to-output example.

--- Judge: z-ai/glm-5.3 | score 74.0/100 ---
[reasoning] As a prompt-design artifact this is well above average. Strengths: a clear role and input scope ('use only the supplied task reward packet'); an unusually strong trust-boundary section that splits deterministic system metadata from user-authored text, treats titles, reward_reasons, and quoted evidence as untrusted data, neutralizes embedded instructions, and converts injection attempts into a scoring penalty; wallet-routing protection; a deterministic eligibility rule (no reward_paid_pft greater than 0 in the lookback → 0 PFT and ineligible); explicit acknowledgment that the host system clamps proposals to max_daily_pft and max_reward_fraction (defense in depth); and a typed JSON output schema with an enum and null handling. The economic north-star heuristic gives the model a genuine decision principle. Weaknesses: the two numeric outputs are uncalibrated — retention_value_score (0-100) has no rubric or anchor bands, and daily_airdrop_pft (0-10000) has bounds but no mapping rule from evidence or score to amount, so cross-contributor consistency and fairness rest on unanchored model discretion; the hard-coded 0-10000 range can also conflict with packet-configured max_daily_pft; there is no aggregation rule for multiple rewarded tasks, no malformed/missing-field handling beyond the zero-reward case, no few-shot examples, and the injection-penalty rule lacks a carve-out for contributors who legitimately quote malicious text as their work product (e.g., security reports). Whether the model actually resists injections, emits parseable JSON, or scores consistently are empirical claims requiring output testing; this score judges design only: strong security and structure, but an uncalibrated decision core.
[best_thing] The trust-boundary and prompt-injection design: a clean split between deterministic system metadata and untrusted user-authored text, explicit neutralization of embedded instructions, turning injection attempts into a score penalty, wallet-routing protection, and acknowledgment of host-side deterministic clamping — genuine defense-in-depth that most decisioning prompts lack.
[worst_thing] The core numeric decision is uncalibrated: retention_value_score has no rubric or anchor bands, and daily_airdrop_pft has bounds but no rule mapping evidence or score to an amount, leaving the actual financial output to unanchored model discretion (with a hard-coded 0-10000 range that can conflict with packet-configured max_daily_pft).
[one_best_next_edit] Add a calibration section: anchored bands defining what low/mid/high retention_value_score look like in terms of evidence quality, artifact value, and follow-through, plus an explicit rule mapping score and packet reward totals to a daily_airdrop_pft proposal within max_daily_pft and max_reward_fraction.

--- Judge: z-ai/glm-5.3 | score 76.0/100 ---
[reasoning] This is a well-above-average production prompt. Design strengths visible from the text alone: a clear role and data scope (use only the supplied packet); a crisp, testable eligibility rule (no reward_paid_pft greater than 0 in the lookback window forces 0 PFT and ineligible); a well-typed output contract with enums and evidence-grounded explanation fields that create self-justification pressure; explicit awareness that the system deterministically clamps to max_daily_pft and max_reward_fraction (defense in depth, so model error is bounded); and an unusually strong trust-boundary section that partitions deterministic system metadata from user-authored text, enumerates specific injection patterns to ignore, and converts injection attempts into a negative scoring signal, turning adversarial behavior into evidence. Main design gaps: (1) the core decision variable, daily_airdrop_pft, has no defined relationship to retention_value_score, total_reward_paid_pft, or the caps, so the model must invent a mapping, threatening cross-contributor fairness and run-to-run consistency for a money-moving decision; (2) the 0-100 retention score has no rubric, anchors, or band descriptions, a known driver of LLM scoring noise; (3) edge cases are unspecified: whether ineligible forces retention_value_score to 0, when eligibility_reason is null versus a string, and behavior on missing or contradictory packet fields; (4) the blanket rule that instruction-like embedded text must lower the score can misfire on legitimate security or verification work whose shipped artifacts contain injection-like payloads, work the prompt elsewhere rewards; (5) no worked example to stabilize behavior. Claims that would require empirical testing rather than reading: actual score variance across runs, real-world injection resistance, JSON validity rates, and whether models anchor near the caps. These are plausible risks, not measured facts. Net: strong on safety, structure, and grounding; weak on calibration for the payout decision that is its entire purpose.
[best_thing] The trust-boundary section: it cleanly separates deterministic system metadata from user-authored untrusted text, enumerates concrete injection patterns to ignore, hard-caps output at packet-justified amounts, and converts injection attempts into a negative scoring signal, turning adversarial behavior into evidence rather than merely defending against it.
[worst_thing] The prompt never defines how retention_value_score (0-100) maps to daily_airdrop_pft (0-10000), nor provides any rubric anchoring the score itself, so the actual payout decision, the prompt's whole purpose, is left to unconstrained model discretion, undermining consistency across contributors and runs.
[one_best_next_edit] Add a calibration section that gives 4-5 anchor bands for retention_value_score (describing what roughly 20, 50, and 80 look like in evidence quality, replaceability, and shipped value) plus an explicit proposal rule making daily_airdrop_pft a function of the score and packet facts, e.g., banded fractions of total_reward_paid_pft clamped by max_daily_pft and max_reward_fraction, and specify that ineligible implies score 0 and when eligibility_reason is null.