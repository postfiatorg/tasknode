import pg from "/app/node_modules/pg/lib/index.js";
import {createHash} from "node:crypto";
const client=new pg.Client({connectionString:process.env.DATABASE_URL,query_timeout:60000});
const before="      SELECT\n        alloc.id AS allocation_id,\n        alloc.project_id,\n        alloc.allocation_status,\n        alloc.allocation_status AS allocation_mirror_status,\n        alloc.task_request_id,\n        alloc.generated_task_id,\n        alloc.project_need_summary,\n        alloc.reward_min_pft,\n        alloc.reward_max_pft,\n        alloc.expires_at,\n        alloc.updated_at,\n        job.id AS generation_job_id,\n        job.status AS generation_status,\n        job.request_id,\n        COALESCE(NULLIF(proj.task_id, ''), NULLIF(refs.task_id, ''), NULLIF(job.task_id, ''), NULLIF(alloc.generated_task_id, '')) AS task_id,\n        proj.status AS projection_status,\n        COALESCE(NULLIF(proj.status, ''), NULLIF(refs.state, '')) AS task_status,\n        COALESCE(NULLIF(proj.title, ''), NULLIF(refs.title, ''), NULLIF(alloc.project_need_summary, '')) AS title,\n        proj.reward_offer_pft,\n        proj.reward_actual_pft,\n        proj.accept_by,\n        proj.deadline_at,\n        proj.updated_at AS projection_updated_at\n      FROM network_task_allocations alloc\n      LEFT JOIN network_task_generation_jobs job\n        ON job.allocation_id = alloc.id\n      LEFT JOIN network_project_task_refs refs\n        ON ((alloc.generated_task_id <> '' AND refs.task_id = alloc.generated_task_id) OR (alloc.generated_task_id = '' AND alloc.task_request_id <> '' AND refs.request_id = alloc.task_request_id))\n      LEFT JOIN task_projections proj\n        ON ((alloc.generated_task_id <> '' AND proj.task_id = alloc.generated_task_id) OR (alloc.generated_task_id = '' AND alloc.task_request_id <> '' AND proj.request_id = alloc.task_request_id))\n      WHERE ($1::text <> '' AND alloc.candidate_account_id = $1)\n         OR ($2::text <> '' AND alloc.candidate_wallet_address = $2)\n         OR ($1::text <> '' AND proj.account_id = $1)\n         OR ($2::text <> '' AND proj.subject_wallet = $2)\n      ORDER BY alloc.updated_at DESC, alloc.id DESC\n      LIMIT $3\n";
const after="      SELECT\n        alloc.id AS allocation_id,\n        alloc.project_id,\n        alloc.allocation_status,\n        alloc.allocation_status AS allocation_mirror_status,\n        alloc.task_request_id,\n        alloc.generated_task_id,\n        alloc.project_need_summary,\n        alloc.reward_min_pft,\n        alloc.reward_max_pft,\n        alloc.expires_at,\n        alloc.updated_at,\n        job.id AS generation_job_id,\n        job.status AS generation_status,\n        job.request_id,\n        COALESCE(NULLIF(proj.task_id, ''), NULLIF(refs.task_id, ''), NULLIF(job.task_id, ''), NULLIF(alloc.generated_task_id, '')) AS task_id,\n        proj.status AS projection_status,\n        COALESCE(NULLIF(proj.status, ''), NULLIF(refs.state, '')) AS task_status,\n        COALESCE(NULLIF(proj.title, ''), NULLIF(refs.title, ''), NULLIF(alloc.project_need_summary, '')) AS title,\n        proj.reward_offer_pft,\n        proj.reward_actual_pft,\n        proj.accept_by,\n        proj.deadline_at,\n        proj.updated_at AS projection_updated_at\n      FROM network_task_allocations alloc\n      LEFT JOIN network_task_generation_jobs job\n        ON job.allocation_id = alloc.id\n      LEFT JOIN network_project_task_refs refs\n        ON ((alloc.generated_task_id <> '' AND refs.task_id <> '' AND refs.task_id = alloc.generated_task_id) OR (alloc.generated_task_id = '' AND alloc.task_request_id <> '' AND refs.request_id <> '' AND refs.request_id = alloc.task_request_id))\n      LEFT JOIN task_projections proj\n        ON ((alloc.generated_task_id <> '' AND proj.task_id <> '' AND proj.task_id = alloc.generated_task_id) OR (alloc.generated_task_id = '' AND alloc.task_request_id <> '' AND proj.request_id <> '' AND proj.request_id = alloc.task_request_id))\n      WHERE ($1::text <> '' AND alloc.candidate_account_id = $1)\n         OR ($2::text <> '' AND alloc.candidate_wallet_address = $2)\n         OR ($1::text <> '' AND proj.account_id = $1)\n         OR ($2::text <> '' AND proj.subject_wallet = $2)\n      ORDER BY alloc.updated_at DESC, alloc.id DESC\n      LIMIT $3\n";
await client.connect();
try{
 await client.query("BEGIN ISOLATION LEVEL REPEATABLE READ READ ONLY");
 await client.query("SET LOCAL statement_timeout=60000");
 const results=[];
 for(const params of [["acct_oauth_3c70e69ab7b8ef1fad3df508","rPo8GkCA9YMKzuJGTHbj11kdVfPqSJHxNx",8],["no_such_hive_recovery_account","",8]]){
  let start=Date.now();const oldRows=(await client.query(before,params)).rows;const beforeMs=Date.now()-start;
  start=Date.now();const newRows=(await client.query(after,params)).rows;const afterMs=Date.now()-start;
  const hash=rows=>createHash("sha256").update(JSON.stringify(rows.map(row=>JSON.stringify(row)).sort())).digest("hex");
  results.push({rows:oldRows.length,beforeMs,afterMs,beforeHash:hash(oldRows),afterHash:hash(newRows),identical:hash(oldRows)===hash(newRows)});
 }
 console.log(JSON.stringify({observedAt:new Date().toISOString(),productionMutations:0,results},null,2));
 if(results.some(r=>!r.identical))process.exitCode=1;
}finally{await client.query("ROLLBACK");await client.end();}
