services:
  api:
    network_mode: host
    ports: !reset []
    extra_hosts: !reset []
    depends_on: !reset []
    environment:
      DATABASE_URL: ${TASKNODE_DATABASE_URL}
      TASKNODE_FLY_DEV_DATA_BRIDGE: "true"

  board-manager:
    network_mode: host
    ports: !reset []
    extra_hosts: !reset []
    depends_on: !reset []
    environment:
      DATABASE_URL: ${TASKNODE_DATABASE_URL}
      TASKNODE_FLY_DEV_DATA_BRIDGE: "true"

  web:
    network_mode: host
    ports: !reset []
    depends_on: !reset []
    environment:
      TASKNODE_API_ORIGIN: http://127.0.0.1:8080
