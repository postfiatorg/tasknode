---
- name: Deploy Task Node
  hosts: task-node-1
  become: yes
  
  pre_tasks:
    - name: Update dnf cache
      dnf:
        update_cache: yes
        cache_valid_time: 3600

  roles:
    - common
    - python
    - postgres
    - tasknode 